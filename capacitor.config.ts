import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.victorsvlynch.reproiptvperu',
  appName: 'Reproductor IPTV Peru',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
